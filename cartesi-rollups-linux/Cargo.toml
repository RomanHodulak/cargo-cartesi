[package]
name = "cartesi-rollups-linux"
version = "0.1.0"
edition = "2021"

[dependencies]
cartesi-rollups = { path = "../cartesi-rollups" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
hex = "0.4"
log = "0.4"
nix = "0.26"
